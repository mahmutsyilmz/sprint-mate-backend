package com.sprintmate.model;

/**
 * Enum defining available role types in the system.
 */
public enum RoleName {
    /** Frontend developer role */
    FRONTEND,
    /** Backend developer role */
    BACKEND
}
