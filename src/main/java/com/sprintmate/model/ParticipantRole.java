package com.sprintmate.model;

/**
 * Enum defining the participant's role in a match.
 */
public enum ParticipantRole {
    /** Participant works on frontend development */
    FRONTEND,
    /** Participant works on backend development */
    BACKEND
}
